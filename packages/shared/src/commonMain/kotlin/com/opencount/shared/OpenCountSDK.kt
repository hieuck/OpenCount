package com.opencount.shared

/**
 * OpenCountSDK - Cross-platform shared business logic
 * 
 * This module contains all core business logic shared across:
 * - iOS (SwiftUI) via Kotlin/Native framework
 * - Android (Jetpack Compose) via AAR
 * - Desktop (Compose for Desktop) via JAR
 * - Web (Kotlin/JS) via JS library
 */
object OpenCountSDK {
    const val VERSION = "1.0.0"
    const val NAME = "OpenCount"
}
