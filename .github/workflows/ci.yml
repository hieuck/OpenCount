# ─────────────────────────────────────────────────────────────────────────────
# CI — Build & Test
# Builds for iOS Simulator. Downloads iOS platform if not present.
# ─────────────────────────────────────────────────────────────────────────────
name: CI — Build & Test

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  SCHEME: OpenCount
  DERIVED_DATA: ${{ github.workspace }}/.build/DerivedData
  XCODE_VERSION: "16.2"

jobs:
  build-and-test:
    name: Build & Test (Simulator)
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode ${{ env.XCODE_VERSION }}
        run: sudo xcode-select -s /Applications/Xcode_${{ env.XCODE_VERSION }}.app/Contents/Developer

      - name: Cache SPM
        uses: actions/cache@v4
        with:
          path: |
            ${{ github.workspace }}/.build/DerivedData/SourcePackages
            ~/.swiftpm
          key: spm-${{ runner.os }}-${{ hashFiles('**/Package.resolved') }}
          restore-keys: spm-${{ runner.os }}-

      - name: Install tools
        run: brew install xcodegen xcbeautify || true

      - name: Download iOS platform
        run: |
          # Download iOS 18 simulator runtime if not already present
          xcodebuild -downloadPlatform iOS 2>/dev/null || true
          # List available simulators
          xcrun simctl list runtimes

      - name: Generate Xcode project
        run: xcodegen generate --spec project.yml

      - name: Find simulator destination
        id: sim
        run: |
          # Find best available iPhone simulator (prefer iPhone 16, fallback to any iPhone)
          DEST=$(xcrun simctl list devices available -j | python3 -c "
          import json, sys
          data = json.load(sys.stdin)
          candidates = []
          for runtime, devices in data['devices'].items():
              if 'iOS' not in runtime:
                  continue
              for d in devices:
                  if d.get('isAvailable') and 'iPhone' in d['name']:
                      candidates.append((runtime, d))
          if not candidates:
              print('platform=iOS Simulator,name=iPhone 16,OS=latest')
              sys.exit(0)
          # Prefer newer runtime
          candidates.sort(key=lambda x: x[0], reverse=True)
          best = candidates[0][1]
          print(f'platform=iOS Simulator,id={best[\"udid\"]}')
          " 2>/dev/null || echo "platform=iOS Simulator,name=iPhone 16,OS=latest")
          echo "destination=$DEST" >> $GITHUB_OUTPUT
          echo "Using destination: $DEST"

      - name: Build (Simulator)
        run: |
          set -o pipefail
          xcodebuild \
            -project OpenCount.xcodeproj \
            -scheme "$SCHEME" \
            -destination "${{ steps.sim.outputs.destination }}" \
            -derivedDataPath "$DERIVED_DATA" \
            CODE_SIGNING_ALLOWED=NO \
            SUPPORTS_MACCATALYST=NO \
            ASSETCATALOG_COMPILER_SKIP_APP_STORE_DEPLOYMENT=YES \
            build \
          | xcbeautify --renderer github-actions || \
          xcodebuild \
            -project OpenCount.xcodeproj \
            -scheme "$SCHEME" \
            -destination "${{ steps.sim.outputs.destination }}" \
            -derivedDataPath "$DERIVED_DATA" \
            CODE_SIGNING_ALLOWED=NO \
            SUPPORTS_MACCATALYST=NO \
            ASSETCATALOG_COMPILER_SKIP_APP_STORE_DEPLOYMENT=YES \
            build

      - name: Run Tests (Simulator)
        run: |
          set -o pipefail
          xcodebuild \
            -project OpenCount.xcodeproj \
            -scheme "$SCHEME" \
            -destination "${{ steps.sim.outputs.destination }}" \
            -derivedDataPath "$DERIVED_DATA" \
            -resultBundlePath "$DERIVED_DATA/TestResults.xcresult" \
            CODE_SIGNING_ALLOWED=NO \
            SUPPORTS_MACCATALYST=NO \
            ASSETCATALOG_COMPILER_SKIP_APP_STORE_DEPLOYMENT=YES \
            test \
          | xcbeautify --renderer github-actions || \
          xcodebuild \
            -project OpenCount.xcodeproj \
            -scheme "$SCHEME" \
            -destination "${{ steps.sim.outputs.destination }}" \
            -derivedDataPath "$DERIVED_DATA" \
            -resultBundlePath "$DERIVED_DATA/TestResults.xcresult" \
            CODE_SIGNING_ALLOWED=NO \
            SUPPORTS_MACCATALYST=NO \
            ASSETCATALOG_COMPILER_SKIP_APP_STORE_DEPLOYMENT=YES \
            test

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: BuildResults-${{ github.run_number }}
          path: ${{ env.DERIVED_DATA }}
          retention-days: 7
